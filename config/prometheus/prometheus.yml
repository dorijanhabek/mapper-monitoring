global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['kai-alertmanager:9093']

rule_files:
  - "rules/*.yml"

scrape_configs:
  - job_name: 'kai-scrape-node'
    static_configs:
      - targets: ['kai-node-exporter:9100']